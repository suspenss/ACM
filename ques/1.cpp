#include <algorithm>
#include <iostream>
#include <vector>

using i64 = int64_t;

void sol() {}

int main() {
    std::cin.tie(nullptr)->sync_with_stdio(false);

    int t = 1;
    // std::cin >> t;

    for (int i = 0; i < t; i++) {
        sol();
    }

    return 0;
}
