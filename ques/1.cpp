#include <algorithm>
#include <iostream>
#include <set>
#include <vector>

using i64 = int64_t;

void sol() {}

int main() {
    std::cin.tie(nullptr)->sync_with_stdio(false);

    int Tests = 1;
    std::cin >> Tests;

    while (Tests--) {
        sol();
    }

    return 0;
}