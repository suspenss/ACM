#include <bits/stdc++.h>
using namespace std;

int main () {
	int n, m, j = 0, t = 0;
	scanf("%d%d", &m, &n);
	j = (4 * m - n) / 2;
	t = m - j;
	cout << j << " " << t;
	// stack<int> s;
	// s.push(1);
}